{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "The overlay renders state pushed from Rust and can ask to open the finished session.",
  "windows": ["overlay"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten", "allow-open-session"]
}
